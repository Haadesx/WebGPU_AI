{
  "name": "edge-ai-benchmark",
  "version": "1.0.0",
  "private": true,
  "description": "Edge AI: Running Quantized LLMs in the Browser (WebGPU) — Comparative Benchmark Harness",
  "authors": [
    "Ethan Nguyen",
    "Varesh Patel",
    "Najeeb Quadri"
  ],
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "server": "npm run dev --workspace=apps/server",
    "bench": "npx tsx scripts/bench.ts",
    "build": "npm run build --workspace=apps/web",
    "install:all": "npm install"
  },
  "devDependencies": {
    "@vercel/node": "^5.6.9",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "openai": "^6.25.0"
  }
}
